# CHANGES_ARCHIVE — dharness 변경 이력 아카이브

> [`CLAUDE.md`](./CLAUDE.md) "변경 이력" 표에서 분리된 아카이브. 신규 사용자는 [`README.md`](./README.md)와 [`plugins/harness/skills/harness/SKILL.md`](./plugins/harness/skills/harness/SKILL.md)를 단일 출처로 보세요. 본 파일은 *어떻게 여기까지 왔는가* 회고 시에만 참고합니다.
>
> **현재 아키텍처는 단계 A 이후 단순화된 형태입니다** — `harness` plugin (외부 install) + dharness self-host CM (root `.claude/` + `_workspace/`, 결정적 hook 3 + skill 1 + commands 5). 아래 표 행은 **append-only history**로, 폐기된 산출물(cm-harness plugin, Phase 10 baseline/diagnostic, cm-orchestrator/cm-curator/cm-digester, dashboard worker, daily\_summary 자동 생성, tool-output-compress 등)을 언급하는 행이 다수 있습니다.
>
> **본 아카이브 범위**: 2026-05-09 초기 구성부터 2026-05-10 Phase 5-2 7차 사이클까지 (27행). 그 이후 행은 [`CLAUDE.md`](./CLAUDE.md) 본문에 유지 — 최근 활동만 노출, *과거 milestone 회고*는 본 archive로.
>
> **검색 도움말 (memory-search SKILL source 3 정합):** `git log`로 commit 매핑이 가능하며, 본 archive를 `grep` 또는 `Read`로 직접 조회. `CLAUDE.md` 본문과 archive는 *append-only*로 시간순 정합 — `2026-05-10` 이전은 항상 archive, 이후는 본문 또는 archive 양쪽 모두 가능 (cut-off 시점에 본문에서 archive로 이주된 행 위주).

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-05-09 | 초기 구성 | 전체 (S1-S6 정의) | context-management 도메인 harness 신규 구축 |
| 2026-05-09 | S7 완료 — CM 전용 Phase 10 진단 룰 추가 | `_workspace/references/cm-diagnostic-rules.md`, `_workspace/_baseline/cm_baseline.json`, cm-orchestrator 스킬 | Phase 10: CM drift 자체 진화 회로 완성 |
| 2026-05-10 | 갭 9건 해소 (점검 결과 반영) | session-capture 스킬 신설, session-digest를 단일 DB 스키마 진실 원천으로 통합, dashboard-render/memory-search SQL 정합화, memory-curate에 daily_summary + 주기 트리거 추가, cm-injector daily_summaries 우선 입력, cm-curator 책임 확장, /cm-* 7종 커맨드 + 핸들러 스크립트 신설, FastAPI 워커 골격 작성, 훅 INSTALL.md 가이드 | 레퍼런스 line 168-176 정합 + 실행 가능성 확보 |
| 2026-05-10 | Skill memory 승격을 Phase 10 rollback chain에 명시적으로 통합 | `_workspace/references/cm-diagnostic-rules.md` §4 표에 "Skill memory 승격" 행 + Atomic 적용 절차 추가, `.claude/skills/memory-curate/SKILL.md` 승격 프로세스를 표준 `_telemetry/_rollback/{ts}/` 인프라 사용으로 구체화 | 승격 시 다중 산출물(skill 파일/cluster md/DB/CLAUDE.md) chain의 dangling rollback 위험 제거 |
| 2026-05-10 | dharness 본체 read-only 경계 invariant 3곳 추가 | `.claude/agents/cm-curator.md` 작업 원칙, `_workspace/references/cm-diagnostic-rules.md` §4 범위 외 각주, `.claude/skills/cm-orchestrator/SKILL.md` Phase 10 연동 영구 범위 한정 섹션 | CM 자동 적응(Phase 10)이 dharness 메타 스킬 본체(`skills/harness/`, `commands/harness-*`)를 침범할 경로를 명시적으로 차단. dharness 일반화 가치가 있는 신호는 Phase 9(`/harness-evolve`)로만 진입 |
| 2026-05-10 | 전체 review 결과 BLOCKER 8 + MAJOR 11 정합성 픽스 (Phase 9: 사용자 피드백 — 전체 파일 리뷰) | `_workspace/_hooks/_schema.py` 신설(DDL+session_id 헬퍼), `_hooks/{session_start,post_tool_use,session_end,cm_commands}.py` 재작성(파일 기반 sid + stdout 컨트랙트 정합 + TZ + dict 직렬화 + DDL import), `_hooks/INSTALL.md` matcher/`${CLAUDE_PROJECT_DIR}` 보강, `commands/cm-curate.md` Task 도구 호출로 정정, `commands/cm-reset.md` 약속 단순화, `commands/harness-new.md` Phase 0-8+10 명시, `cm-orchestrator/SKILL.md` 가상 API→Task 도구로 교체, `session-capture/SKILL.md` 환경변수→파일 기반 명시, `session-digest/SKILL.md` 진실 원천 2계층 명시, `tool-output-compress/SKILL.md` margin 룰 정리, `memory-curate/SKILL.md` member_observations 권위 출처 명시, `cm-injector.md` description 정정 + 5 cm-* 에이전트에 `tools:` allowlist, `_baseline/cm_baseline.json` notes/user_confirmed_skills 정렬, `_baseline/project_profile.md` detection_signals 갱신, `_worker/dashboard_server.py` XSS escape + mtime 캐시 무효화, `harness/SKILL.md` Phase 8 단계 8-1~8-7 명시 + should-trigger 10+10 통일, `runtime-adaptation.md` 8 → 10+10 정렬 | 4-그룹 병렬 리뷰에서 발견한 hook 컨트랙트 위반·DDL 진실원천 우회·가상 API·schema drift·XSS 등 정합성 버그 일괄 해소. dharness 본체 read-only invariant는 보존 (skills/harness/SKILL.md의 Phase 8 단계 번호 정합화는 사용자 직접 수정 영역) |
| 2026-05-10 | 정합성 11건 일괄 정정 (Phase 9: 사용자 피드백 — 전체 폴더 review) | `CLAUDE.md`(Phase 7-4 포인터화 — 카탈로그·구현 현황·진단 룰 위치 README/skills 단일 출처로 위임) + `harness/SKILL.md` Phase 7-4 템플릿 + `cm-orchestrator/SKILL.md`(Phase 10 mtime-anchor 도입 + 라우팅 표 "훅 자율" 컬럼 분리) + `cm-digester.md` `cm-curator.md`(SendMessage→Task 반환값/prompt payload 정정 + obs_id 형식 통일 + SessionEnd 호출 타이밍 명시) + `session-capture/SKILL.md`(에이전트는 파일 직접 read, 스크립트만 `_schema.read_session_id()`) + `commands/cm-init.md`(baseline json 자동 생성 항목 제거) + `cm_commands.py`(미사용 `daily_summaries/` 디렉토리 생성 제거) + `intent_profile.md`(S6 dashboard 워커 open_question을 Resolved로 이전) + `session_start.py`(session_id 충돌 시 orphan 디렉토리 cleanup) | (1) Phase 10 카운터가 발행되지 않는 `type:adapt`에 의존하던 spec drift 해소 — `_delta_*.md`/`_rollback/{ts}/` mtime을 anchor로 사용. (2) 메시지 버스 부재 환경에서 SendMessage 잔재 정정. (3) CLAUDE.md가 자체 Phase 7-4 룰 위반(전체 카탈로그 중복 보유)을 자기 시연으로 해소. (4) 코드와 spec의 micro-drift 6건 동기화. dharness 본체(`skills/harness/SKILL.md` Phase 7-4 템플릿)는 사용자 명시 요청 — Phase 9 영역. |
| 2026-05-10 | 멀티 프로젝트 dashboard (옵션 A — 분산 DB) + Inventory/Roadmap View 5 | `_workspace/_hooks/_schema.py`(REPO_ROOT를 `CLAUDE_PROJECT_DIR` 우선으로) + `_workspace/projects.json` 신설(수동 프로젝트 레지스트리) + `_worker/dashboard_server.py` 재작성(프로젝트별 routes + inventory 스캐너 + CLAUDE.md 표 파서 + StaticFiles `/ui/` mount + localhost CORS) + `dashboard-render/SKILL.md`에 옵션 A 모델 + View 5 + 신규 엔드포인트 명세 추가 | dharness 하네스가 적용된 여러 프로젝트의 세션·tools·축적 정보를 한 화면에서 비교 열람. 프론트엔드는 Claude design 산출물을 `_workspace/_worker/static/`에 mount하여 `/ui/`로 서빙. cross-project SQL JOIN 없음 — 각 프로젝트 DB 독립 조회. dharness 본체 read-only invariant 보존 |
| 2026-05-10 | LLM 단계 사슬 끊김 픽스 (영속성/압축 점검 결과 반영) | `_workspace/_hooks/session_start.py`(dangling 세션 자동 finalize + `digest_path IS NULL` 후보를 `[CM Backfill]` 신호로 additionalContext에 포함) + `_workspace/_hooks/session_end.py`(`clear_session_id()` 제거 — `unknown` 누수 방지) + `cm-orchestrator/SKILL.md`(라우팅 표에 SessionStart digest backfill 행 추가 + cm-digester 다회→cm-curator 1회→cm-injector 순차 dispatch 절차 명시, SessionEnd 후속은 다음 SessionStart로 이월) | 점검 결과 cm-digester/cm-curator/cm-compressor가 0회 실행되어 observations·clusters·daily_summaries 모두 0행. 원인은 SessionEnd 훅이 LLM 단계를 트리거할 창구가 없는 구조적 결함. SessionStart의 `additionalContext` 채널을 통해 직전 세션 digest 미생성을 자동 backfill하도록 사슬 복구. dharness 본체 read-only invariant 보존 |
| 2026-05-10 | Plugin 매니페스트 도입 — `/plugin install` 가능한 형태로 전환 (Step 1+1.6) | `.claude-plugin/plugin.json` 신설 (skills 2종/commands/agents/hooks 경로 선언) + `.claude-plugin/marketplace.json` 신설 (github source 단일 plugin 카탈로그) + `_workspace/_hooks/hooks.json` 신설 (`${CLAUDE_PLUGIN_ROOT}` 기반 3종 훅 등록) + `README.md` "다른 프로젝트에 dharness 도입하기"에 §0 (권장) Plugin 설치 섹션 추가 (A/B 절차는 보존) | 디렉토리 구조 변경 0, 코드 수정 0줄. 기존 `_schema.py`가 이미 `CLAUDE_PROJECT_DIR` env var로 데이터 경로를 잡고 있어서 plugin model과 자연 호환. 사용자가 `settings.json` 직접 편집 없이 `/plugin install dharness@dharness` 한 줄로 도입 가능. 메타 스킬과 CM 산출물이 한 plugin에 묶여 선택 install이 안 되는 한계는 Step 2(plugin 분리)에서 해소 예정 |
| 2026-05-10 | Step 2 — monorepo subdirs 모델로 두 plugin 분리 (Step 2.1~2.5) | `plugins/harness/` 신설 (메타 스킬 + harness-* commands 이동) + `plugins/cm-harness/` 신설 (cm-* agents/skills/commands + hooks/ + worker/ + references/ 이동) + 각자 `.claude-plugin/plugin.json` + `plugins/cm-harness/hooks/hooks.json` 경로를 plugin-relative(`${CLAUDE_PLUGIN_ROOT}/hooks/...`)로 정정 + `_schema.py`에 `_walk_up_for_workspace()` fallback 추가 (manual self-use에서 `_workspace/` 디렉토리를 가진 가장 가까운 조상 탐색) + 기존 root `.claude-plugin/plugin.json` 삭제 + `marketplace.json`을 두 plugin git-subdir 카탈로그로 확장 + `.claude/settings.local.json` 훅 경로 갱신 (gitignore이라 commit에 미포함) + 기존 `commands/README.md`, `skills/README.md` 삭제 + `README.md` 전면 갱신 + `CLAUDE.md` 최상단 산출물 카탈로그 갱신 | 사용자가 "메타만" 또는 "CM만" 또는 "둘 다" 선택 install 가능. `harness:harness-*` vs `cm-harness:cm-*` 네임스페이스 분리. `plugins/`(코드, install 시 read-only) ↔ `_workspace/`(데이터, 사용자 프로젝트별 격리) invariant가 디렉토리 경계로 자연 강제됨. plugin 내부 SKILL.md/agent.md 파일들이 옛 경로(`_workspace/_hooks/...`)를 가리키는 잔존 참조는 다음 단계(Step 2.6)에서 일괄 갱신 |
| 2026-05-10 | Step 2.6 — plugin 내부 잔존 참조 일괄 갱신 (Step 2 완전 완료) | 29개 파일에서 옛 경로/네임스페이스 일괄 치환: (a) `_workspace/_hooks/` → `plugins/cm-harness/hooks/`, `_workspace/_worker/` → `plugins/cm-harness/worker/`, `_workspace/references/cm-diagnostic-rules.md` → `plugins/cm-harness/references/cm-diagnostic-rules.md` (단, `_workspace/_baseline,_telemetry,_memory,_tool_outputs,projects.json`은 데이터 경로이므로 KEEP) + (b) cm-harness self-ref `.claude/agents/cm-*` → `plugins/cm-harness/agents/cm-*`, `.claude/skills/{cm-orchestrator|memory-curate|tool-output-compress|session-capture|session-digest|dashboard-render|memory-search}` → `plugins/cm-harness/skills/{name}` + (c) 슬래시 커맨드 네임스페이스 102건 (`/harness-X` → `/harness:harness-X`, `/cm-X` → `/cm-harness:cm-X`) + (d) harness self-ref `skills/harness/` → `plugins/harness/skills/harness/`, Pass A에서 삭제된 `commands/README.md` 잔존 참조 → `README.md` "Slash command 카탈로그" 섹션 포인터로 교체 + (e) `INSTALL.md` 매뉴얼 hook 경로를 `${CLAUDE_PROJECT_DIR}/plugins/cm-harness/hooks/...`로 정정 | install 동작에는 영향 없으나 SKILL/agent가 LLM 호출 시 잘못된 경로를 안내할 위험을 해소. dharness 본체 read-only invariant는 보존 (메타 harness skill 안의 일반 placeholder `.claude/{agents,skills}/`는 derived 프로젝트 대상이므로 의도적 유지). cm-harness 도메인 자체 적응 chain의 변경 대상 매핑이 새 경로로 정합화. 단일 commit, smoke test로 잔존 0건 확인 + JSON manifest 4종 + Python `_schema.py`/`cm_commands.py` import 정상 검증 |
| 2026-05-10 | CM 수술적 재정비 — Phase 10/Orchestrator/agents 폐기, 결정적 모델로 축소 (Tier 1+2+3A) | (Tier 1) `_workspace/_baseline/`, `_telemetry/_rollback/`, `cm-diagnostic-rules.md`, debug `.curator_run.py`, leaked `_tool_outputs/unknown/` 삭제 (492줄). (Tier 2) cm-* 5 agents + cm-orchestrator/session-capture/session-digest/memory-curate/dashboard-render/tool-output-compress 6 skills + cm-curate/cm-clusters/cm-init 3 commands 삭제 (1558줄). (Tier 3A) `session_start.py`에서 `[CM Backfill]` LLM 분기 제거 + 최신 daily_summary 1건 inject로 교체, `session_end.py`/`post_tool_use.py` 잔존 dispatch 메시지 정리, `cm_commands.py`에서 cmd_init/cmd_clusters 제거 + cmd_reset이 사용하는 init 로직을 `_init_storage` private helper로 정리 + `_rollback` 디렉토리 생성 제거, `_schema.py` docstring 자기-진실-원천 명시, `memory-search/SKILL.md`에서 deleted 스킬 참조 제거, README/CLAUDE.md/2 plugin 매니페스트 narrative 갱신 ("5 agents/7 skills/Phase 10" → "3 hooks + 워커 + 4 commands + 1 skill"). | (1) "LLM이 매 SessionStart마다 자발적으로 dispatch chain을 실행해야" 가정 폐기 — 실측 compliance 0/5. (2) Phase 10 baseline/diagnostic/rollback DNA는 dharness 본체의 harness-evolve 채널과 중복되므로 CM-side 진입로 차단 (사용자 결정). (3) data layer (DB/hooks/workspace/dashboard) 100% 보존 — 11 sessions/63 obs/7 clusters/1 daily_summary 무손실. (4) Worker 측 digest job 통합(transcript→Anthropic API→DB)은 후속 작업(Tier 3B)으로 분리 — API 키 정책 사용자 결정 대기. dharness 본체 read-only invariant 보존 — `plugins/harness/`는 미변경 (다만 deleted CM-specific baseline/diagnostic 파일을 구체 예시로 참조하던 9개 reference에 dangling pointer 잔존, 별도 세션에서 정리 예정). |
| 2026-05-10 | 단계 A — 경계 단순화: cm-harness plugin 폐지 + dharness self-host CM으로 강등 | `plugins/cm-harness/{hooks,commands,skills/memory-search,worker}` → root `.claude/{hooks,commands,skills/memory-search}` + `worker/`로 git mv (history 보존). `plugins/cm-harness/.claude-plugin/plugin.json` + `plugins/cm-harness/hooks/{hooks.json,INSTALL.md}` + `_workspace/projects.json` 삭제. `_schema.py` 재작성 — `${CLAUDE_PROJECT_DIR}` 분기 + `_walk_up_for_workspace()` fallback 제거, `REPO_ROOT = Path(__file__).resolve().parents[2]`로 dharness root 결정적 계산. `worker/dashboard_server.py` 단일 프로젝트화 — `load_registry()` / `find_project()` / `_default_project()` 폐기, `view_*(proj)` → `view_*()`, `/api/projects/*` → `/api/{view}` 단순화. `marketplace.json`에서 cm-harness 카탈로그 항목 제거 (harness 1종만). `.claude/settings.local.json` hooks 경로 갱신 (`plugins/cm-harness/hooks/...` → `.claude/hooks/...`). 4 commands + worker README + memory-search SKILL.md + README.md + CLAUDE.md narrative 일괄 갱신 (`/cm-harness:cm-*` → `/cm-*`). | CM의 정체성을 "임의 프로젝트의 컨텍스트 매니저"에서 "dharness 자체의 진화 기록자"로 재정의하기 위한 사전 단순화. plugin 분리 비용(매니페스트 4종, walk-up fallback, projects.json, 멀티 프로젝트 dashboard 분기)이 외부 install 가치보다 크다는 사용자 결정 반영. 데이터(`_workspace/_memory/observations.db` 11 sessions/63 obs 등) 무손실. dharness 본체 read-only invariant 보존 — `plugins/harness/`는 미변경. 단계 B(observations 컬럼 추가) → C(SessionStart inject 의미화) → D(CLAUDE.md draft 회로) → E(memory-search 도메인 한정) 순으로 후속 진행 예정. |
| 2026-05-10 | 단계 B — observations 컬럼 추가 + dharness 도메인 분류기 (PostToolUse 인라인) | `_schema.py`(observations에 `category`/`artifact_kind`/`phase` 컬럼 + 2 인덱스 추가, `ensure_migrations()` ALTER 마이그레이션 idempotent, `classify_dharness_event()` file path/git subcommand 기반 24 룰 분류기 — `_workspace/__pycache__/.git/` 명시 skip) + `post_tool_use.py`(tool_input 파싱 → classify → `INSERT INTO observations ... section='dharness_event'`) + `session_start.py`(DDL 직후 `ensure_migrations()` 호출 — `CREATE TABLE IF NOT EXISTS`는 컬럼 추가 안 하므로 ALTER 필수). | dharness 작업의 단위(skill/agent/hook/command/manifest/CLAUDE.md 변경)를 deterministic으로 적재해 "context manager"의 의미적 누적이 zero-grow였던 문제 해소. LLM 호출 없음. 신규 데이터부터 분류, 기존 observations는 NULL 보존(retro-classify 없음). 18 dharness_event가 이번 세션에 자동 적재됨을 e2e 검증. |
| 2026-05-10 | 단계 C — SessionStart inject 의미화 (deterministic carry-over) | `session_start.py` — `fetch_prior_sessions(conn, current, n=3)` 직전 세션 dharness_event category 카운트 집계 + `fetch_git_status_short(max=12)` `git status --short` 호출(2초 timeout) + `format_inject(...)` 4 블록 패킹(session_id / 직전 세션 사실 set / git status / daily_summary). 토큰 budget 800→2000자, daily_summary 단독 cap 800→600자로 축소(다른 블록 공간 확보). | stale daily_summary 1건만 inject되던 carry-over를 직전 세션 사실 set + 작업 중단점(uncommitted 파일)으로 확장. 모두 git/sqlite/file mtime 기반 deterministic — Tier 3B의 LLM digest job 도입 결정 회피. 단계 B 분류기의 누적이 inject 신호로 직접 가시화 (931자 e2e 검증). |
| 2026-05-10 | 단계 D — CLAUDE.md draft 자동 회로 (slash command 게이트) | `session_end.py`(`_collect_session_events()` + `generate_draft()` — 이번 세션 dharness_event를 markdown row + 본문으로 `_workspace/_drafts/{date}_{sid}.md`에 적재, `artifact_kind='git'` 이벤트는 target에서 제외) + `session_start.py`(`fetch_pending_drafts()` + inject에 5 번째 블록 추가) + `cm_commands.py`(`claudemd-list`/`claudemd-apply <sid>`/`claudemd-discard [sid]` 핸들러, apply 시 CLAUDE.md "변경 이력" 표 마지막 row 다음에 삽입 후 draft를 `_drafts/applied/`로 이동) + `.claude/commands/cm-claudemd-{apply,discard}.md` 신설 + `_schema.py`(`DRAFTS_DIR`/`DRAFTS_APPLIED`/`DRAFTS_DISCARDED`/`CLAUDE_MD` 상수) + `.gitignore`(_workspace/_drafts/{*,applied,discarded}/*.md). | 세션 간 영속성의 마지막 고리 — SessionEnd가 자동 draft 생성, 다음 SessionStart가 inject, 사용자가 `/cm-claudemd-{apply,discard}`로 명시 게이트. 사유 컬럼은 placeholder로 유지하여 사용자가 직접 채우도록 (manual gate 의도). `_find_change_history_table` / `_extract_draft_row` 단위 테스트 + `claudemd-list`/`claudemd-discard` e2e 검증 완료. |
| 2026-05-10 | 단계 E — memory-search 도메인 한정 (dharness 진화 이력 검색) | `.claude/skills/memory-search/SKILL.md` 재작성 — 검색 source 2개(observations + clusters)에서 5개로 확장: ① `observations_fts` ② `observations` WHERE `section='dharness_event'` (단계 B의 `category`/`artifact_kind` 필터) ③ CLAUDE.md "변경 이력" 표 ④ `git log` + `git show` ⑤ `clusters`/`daily_summaries`/skill 본문. category/artifact_kind 카탈로그 명시, 쿼리 패턴 → source 매핑 표, fallback 전략 5건. 적용 범위를 "dharness 본 폴더"로 한정 명시 (외부 프로젝트 메모리는 범위 밖 — 단계 A 이후 cm-harness plugin 폐지). | 단계 B로 적재되는 dharness_event를 자연어로 회고 가능하게 (예: "단계 X 자세히", "최근 schema 변경 이유", "어떤 hook 편집됐어?"). "임의 프로젝트 메모리"라는 plugin-시절 메타포어를 "dharness 자체의 진화 이력"으로 재정의 — 단계 A의 정체성 변경과 정합. 단계 A → E 시리즈의 마지막 commit. |
| 2026-05-10 | Phase 5-2 신설 — 생성 대상 에이전트의 MCP·도구 자동 할당 카탈로그 | `plugins/harness/skills/harness/references/permission-profiles.md` 신설(3-layer 권한 모델 = 서버 toolset 필터 / 서브에이전트 격리 / permissions 게이트 + 4 capability profile = code-test/web-research/external-integration/reasoning-aux + MCP Tier T0/T1/T2 + 매핑 결정 트리 + 합성 산출물 템플릿 3종 = agent frontmatter / .mcp.json / settings.json + 안전 정책 + §8 검증 미완 5건) + `plugins/harness/skills/harness/SKILL.md` Phase 5 끝에 Phase 5-2 포인터 1단락 + `plugins/harness/commands/harness-add-agent.md` Phase 5 step에 5-2 명시 | 에이전트 생성 시 MCP/도구 권한이 수동·즉흥적으로 부여되던 문제를 capability-profile 기반 *제안 후 사용자 confirm* 합성으로 체계화. MCP 도구 정의가 매 세션 컨텍스트에 정적 적재되어 토큰을 소비하는 문제는 3-layer 모델(서버 측 toolset 필터로 advertising 자체 축소 → 서브에이전트 격리로 parent 미적재 → permissions로 호출 게이트)로 분리. 자동 install·자동 `allow` 승급 금지 — T0(무키·로컬) 한정. dharness 본체 read-only invariant 보존 (카탈로그는 *생성 대상 프로젝트*의 .claude/.mcp.json만 건드림). 검증 미완 5건은 §8에 박제 — 후속 PoC 대상. |
| 2026-05-10 | Phase 5-2 PoC + docs 검증 — §8 항목 일부 해소 (1·2차 사이클) | `plugins/harness/skills/harness/references/permission-profiles.md` §5-1 합성 템플릿을 inline `mcpServers:` 패턴으로 갱신(공식 docs 인용: subagent inline 정의 시 parent 미적재) + §3 인벤토리 표를 install 명령·도구 enumeration 컬럼으로 확장 + §8을 8-1(확정 사실)/8-2(미완)/8-3(재사용 검증 기법)로 분리 + §9 신설(plugin-bundled subagent는 `mcpServers`/`permissionMode`/`hooks` 무시 — derived 하네스 패키징 시 주의). 외부 변경: `claude mcp add` 2회로 `fetch`(mcp-server-fetch-typescript, 4 도구) + `sequential-thinking`(1 도구)을 dharness 프로젝트 local 스코프로 install (✓ Connected 양쪽 확인). 2차 사이클: stdio JSON-RPC `tools/list` 직접 핑으로 두 MCP 도구 카탈로그 empirical 검증(source grep과 100% 일치) + GitHub MCP 공식 toolsets 19종 카탈로그 확정(default 5: context/repos/issues/pull_requests/users) + playwright MCP는 toolset이 env 아닌 CLI flag(`--browser`/`--isolated`/`--caps` 등) 형태임을 식별. | (B) Anthropic 공식 docs로 "subagent inline mcpServers → parent 컨텍스트 미적재" 사실 확정 + plugin-subagent 제약 발견. (C) install path / connection / 도구 enumeration / 명명 규칙(`mcp__<server>__<tool>`) 실측. 부수 효과: auto-mode classifier가 §6 자동 install 금지 정책을 즉시 enforce함을 self-test로 확인 (분류기 차단 → 사용자 명시 승인 후 진행). 2차 사이클의 핵심 산출물은 §8-3 재사용 가능 검증 기법(JSON-RPC 직접 핑) — 향후 임의 MCP를 install 없이도 도구 enumerate 가능. dharness 본체 read-only invariant 보존. |
| 2026-05-10 | Phase 5-2 3차 사이클 — git MCP install 검증 + §10 Dynamic MCP Adoption 워크플로우 신설 | `plugins/harness/skills/harness/references/permission-profiles.md` §3 git 행에 install 명령(uvx 절대경로 포함) + 12 도구 enumeration(`git_status`/`git_diff_*`/`git_commit`/`git_add`/`git_reset`/`git_log`/`git_create_branch`/`git_checkout`/`git_show`/`git_branch`) 채움 + §8-2 완료 항목 3건 추가 + §10 신설(트리거 신호 3종 = baseline-diff/profile-mismatch/사용자-요청, 5-step 채택 절차 = discover→pre-install probe(§8-3 재사용)→user confirm gate→install→reflect 4 산출물(§3/agent frontmatter/settings.json/CLAUDE.md), 프로젝트 유형별 채택 매트릭스 5종 = web/data/mobile/research/devops, rollback 절차, 자동화 가능 vs 사용자 게이트 분계). 외부 변경: `py -m pip install --user uv`(이미 설치됨, 0.11.10) + `claude mcp add git C:\...\uvx.exe -- mcp-server-git --repository C:\Users\user01\awesome-files\dharness` (✓ Connected) + JSON-RPC stdio 핑으로 12 도구 카탈로그 empirical 검증. | (1) (α) git MCP PoC 완료 — 1·2차 사이클의 §8-2 미완 5건 중 1건 해소. (2) 사용자가 명시 요청한 "프로젝트 유형 및 진행에 따라 MCP를 새롭게 채택하여 다운로드 및 반영할 수 있는 방법"을 §10으로 단일 출처화 — 합성 시점(Phase 5-2)이 아닌 *런타임 시점*의 도입 절차를 §10으로 분리하고 4 산출물 동시 패치 룰을 명시. (3) uvx-기반 MCP는 `command:`에 절대경로 필수임을 검증(PATH 미통과 시 spawn 실패) — 반복 함정 제거. (4) §10-3 매트릭스는 *제안 후보군*일 뿐 강제 아님 — 사용자 confirm 게이트는 모든 케이스 동일. dharness 본체 read-only invariant 보존 (편집 대상은 plugins/harness/ docs만, 외부 변경은 `~/.claude.json` projects.{dharness}.mcpServers에 격리). |
| 2026-05-10 | Phase 5-2 4차 사이클 — mid-session 비적재 empirical 확정 + §11 실증 테스트 레시피 4종 박제 | `plugins/harness/skills/harness/references/permission-profiles.md` §8-2 신규 완료 항목 1건 추가(mid-session add된 MCP는 본 세션 도구 풀에도 spawn된 서브에이전트 inherit pool에도 미적재 — 양면 empirical 확인) + 미완 항목별로 `§11-N` 레시피 포인터 부착 + §11 신설(11-1 다음 세션 `mcp__<server>__<tool>` 노출 검증 / 11-2 derived 프로젝트의 `.claude/agents/mcp-isolation-probe.md` 작성 + spawn으로 inline `mcpServers:` parent isolation 측정 / 11-3 `enabledMcpjsonServers` 빈 배열로 적재 vs 호출 차단 분기 측정 / 11-4 SQLite 분석 시나리오로 §10 5-step e2e reproducer). 4차 사이클 실측: 동일 세션에서 fetch/sequential-thinking/git 3 MCP가 `claude mcp list ✓ Connected`인데도 부모 측 `ToolSearch "mcp__"` → 0 hit + Explore 서브에이전트 inherit pool 점검 → 0 hit. | (1) §8-1 사실(subagent inherit) 위에 *mid-session add는 propagate 안 됨* 운영 조건 추가 — Phase 5-2 합성 직후 즉시 사용 불가, 사용자에게 "다음 세션부터" 명시 필요. (2) MCP 도구 풀이 세션 시작 시 1회 materialize됨을 양면(부모+서브) 검증으로 확정 → 4차 cycle 핵심 산출. (3) §11 4종 레시피는 외부 환경(다음 세션 / derived 프로젝트 / 재시작 / 임의 도메인) 의존 검증 항목 각각을 *복사 실행 가능*하도록 자기 완결형으로 박제 — §8-2 미완 5건이 산발적 메모에서 reproducer로 승격. dharness 본체 read-only invariant 보존. |
| 2026-05-10 | Phase 5-2 5차 사이클 — §11 4종 레시피를 *복사 실행 fixture*로 승격 (외부 실행자 손길 최소화) | `plugins/harness/skills/harness/references/fixtures/` 신설 — 5종 파일: `README.md`(인덱스 + 결과 로그 누적 표) / `verify_11_1.md`(다음 세션 첫 user 메시지 프롬프트 + 결과 캡처 템플릿 + §3 footnote 갱신 액션) / `mcp-isolation-probe.agent.md`(derived 프로젝트 `.claude/agents/`에 그대로 복사 가능한 ready-to-spawn frontmatter + inline `mcpServers:` + 출력 파싱 형식) / `verify_11_3.md`(베이스라인 N1 → 토글 OFF → 측정 N2 → 분기 판정 → 복원 4단계 + 결과 표 템플릿) / `probe_sqlite.js`(§10 Step 2 pre-install JSON-RPC stdio probe — `mcp_probe_git.js` 동일 패턴, target만 sqlite로 교체, UVX_PATH/DB_PATH만 환경 맞게 조정). 동시에 §11-1·§11-2·§11-3·§11-4 각 섹션 첫 줄에 `▶ Fixture:` 포인터 1행씩 삽입 + §8-2 미완 list 끝에 5차 사이클 진척 인용블럭 1개 삽입(4종 fixture 박제 완료 안내 + 결과 로그 위치 명시). | (1) 4차 사이클의 §11은 *문서 형태 reproducer*에 머물러 외부 실행자가 매번 보일러플레이트(probe 스크립트 작성·agent.md 보일러 작성·세션 비교 절차 정리)를 재유도해야 했음. 5차 사이클은 그 보일러를 *파일로 박제*해 "파일 복사 → 단일 실행 → 결과를 README에 기록" 3단으로 축약. (2) `_fixtures/` 대신 `fixtures/` 명명 — references/ 하위는 canonical 영역이라 leading underscore(데이터/임시 신호) 부적합. (3) dharness invariant 보존 — fixture는 모두 `plugins/harness/skills/harness/references/fixtures/` 내부에 격리. mid-session 직접 실행 0 (session 재시작/derived 프로젝트/MCP install 의존 항목들이라 §6 정책에 따라 외부 실행자 손길 필수). (4) 검증 게이트: 외부 실행자가 결과를 `fixtures/README.md` "결과 로그"에 누적하면 §8-2 미완 4건이 차례로 ✓로 이동 — *진척 가시성*을 단일 파일에 집중. |
| 2026-05-10 | Phase 5-2 6차 사이클 (5-cycle closure A→D→C→B) — 정합성·경계·구체예시·런타임 진입점 묶음 마무리 | (A) `plugins/harness/skills/harness/SKILL.md` Phase 5-2 단락 + `commands/harness-add-agent.md` Phase 5-2 step + `commands/harness-new.md` Phase 3~6 항목 — 5 cycle 누적(§10 dynamic adoption, §11 fixture 4종, mid-session 미전파 운영 함의, plugin-bundled subagent 제약) 일괄 반영. (D) `references/permission-profiles.md` §10·§11 도입부에 *적용 경계* 인용블럭 1개씩 — "본 절차는 derived 프로젝트의 `.claude/` 대상, dharness root는 self-host CM 격리 영역" 명시. (C) `references/fixtures/synthesis_example/` 신설 — `README.md`(시나리오/4 산출물 인덱스/관찰 포인트 3종/적용 경계) + `data-analyst.agent.md`(inline mcpServers sqlite + read-only 강제 정책 + 운영 함의 끝단) + `settings.json`(permissions allow 3종 / ask 2종) + `CLAUDE_md_row.md`(변경 이력 1행 박제 + 컬럼 의미 + append-only rollback). (B) `commands/harness-mcp-adopt.md` 신설 — §10 5-step 진입점, 선조건 3 (기존 하네스/`claude mcp list` 카운트/dharness root 차단), Step 1-5 매핑, 검증 게이트(다음 세션부터), Rollback 4단, 범위 외 명시. SKILL.md/harness-add-agent.md/harness-new.md/README.md `/harness:harness-mcp-adopt` cross-link 8건 추가, README 카탈로그 카운트 7→8로 갱신. | (1) 5 cycle을 거치면서 SKILL.md/명령 파일이 cycle 1-2까지만 반영된 *spec drift*를 일괄 정정 (cycle 3·4·5 누락 미반영분 모두 박제). (2) §10/§11이 "dharness root에 적용되는가"라는 carry-over 메모 수준 정보를 본문에 박제(D) — 외부 install 후 혼동 방지. (3) §5 합성 템플릿이 §10 Step 5 4 산출물로 정합 결합되는 1세트 박제(C) — 외부 도입자가 자신의 도메인으로 매핑할 형태 참고 자료. (4) 런타임 신규 MCP 채택을 *문서*에서 *명령*으로 격상(B) — 사용자가 `/harness:harness-mcp-adopt`로 명시 trigger 가능, description 매칭 확률 의존 제거. (5) dharness 본체 read-only invariant 보존 — 모든 편집은 `plugins/harness/` 내부, 외부 변경 0. |
| 2026-05-10 | CM 단순화 묶음 — dashboard 폐기 + daily\_summary inject 블록 폐지 + in-session 가이드 + 노이즈 정리 (Phase 9: 사용자 피드백 — 종합 review 후속) | (1) `worker/` 디렉토리 전체 + `.claude/commands/cm-dashboard.md` 삭제 + `cm_commands.py`에서 `cmd_dashboard`/`DASHBOARD_URL`/urllib 의존 제거 + `_schema.py`에서 `cm_worker_edit`/`cm_worker_static_edit`/`cm_doc_edit`/`cm_deps_edit` 분류 룰 4건 제거 (legacy 카테고리는 historic data 보존). (2) `CLAUDE.md` "In-session 컨텍스트 가드라인" 섹션 신설 — 5 행동 룰 표 (Agent 위임 / Glob 우선 / git --oneline / git diff --stat / 파일 재read 자제). LLM 행동 가이드임을 명시 (hook 강제 없음). (3) `session_start.py`에서 `fetch_latest_daily_summary` / `DAILY_SUMMARY_MAX_CHARS` / ⑤ 블록 제거 → 4 블록 inject로 단순화 (session\_id / 직전 N=3 세션 / 미적용 draft / git status). `daily_summaries` 테이블 schema·SQL 조회 path는 historic data 보존용으로 유지 (memory-search source #5). (4) `README.md` 70+ 줄 "CM Dashboard 사용법" → "CM 데이터 직접 조회" 단순화 (sqlite CLI + `/cm-status`/`/cm-sessions` 안내). 트러블슈팅·문서 인덱스·프로젝트 구조·slash 카탈로그(14개), CM Manager 결정적 산출물 표를 dashboard 폐기 후 형태로 갱신. (5) memory-search SKILL 카테고리 카탈로그 현행화 + worker/cm\_doc/cm\_deps legacy 명시 + clusters/daily\_summaries §2c에 "신규 row 생성 안 됨" 인용블럭. (6) `CLAUDE.md` 변경 이력 표 위에 aging banner 추가 — "현재 아키텍처는 단계 A 이후 단순화" + 폐기 산출물(cm-harness/Phase 10/cm-orchestrator/cm-curator/cm-digester/dashboard worker/daily\_summary 자동 생성/tool-output-compress) 명시 + 신규 사용자에게 README/SKILL.md 단일 출처 안내. `/cm-status`/`/cm-sessions` 5종으로 축소(`/cm-dashboard` 폐기). | (a) 외부 GUI 의존성(FastAPI/uvicorn) 폐기 — CM이 표준 라이브러리만으로 작동, 외부 install 책임 0, 단계 A→E 정신과 정합. (b) Tier 3B(LLM digest job) 무산 결정 — daily\_summary 자동 생성 인프라 완전 폐기, 결정적 4 블록 inject로 ⑤ stale dead weight 제거. (c) in-session 토큰 절약은 hook이 아니라 워크플로우 룰(CLAUDE.md 가드라인)로 — Claude Code 자동 압축·subagent 격리·도구 truncation에 위임. (d) memory-search legacy 표시로 클러스터/일자 요약이 "조회 가능 historic data"임을 명확화 — 신규 row 미생성 사실 박제. (e) aging banner로 변경 이력 표의 폐기 DNA 언급이 신규 사용자 onboarding 노이즈가 되는 문제 해소. dharness 본체 read-only invariant 보존 (`plugins/harness/` 미변경, qa-agent-guide.md의 generic "dashboard" 단어는 derived 프로젝트 도메인 예시이므로 유지). |
| 2026-05-10 | `/cm-claudemd-apply` 사유 인자 추가 — placeholder silent failure 해소 (Phase 9: 사용자 피드백 — UX 개선) | `_schema.py`에 `DRAFT_REASON_PLACEHOLDER` 상수 단일 출처화 (이전엔 `session_end.py` local). `session_end.py`/`cm_commands.py`가 schema에서 import — placeholder 변경 시 단일 지점만 수정. `cm_commands.py:cmd_claudemd_apply()` 시그니처에 `reason_parts: list[str]` 추가 + argparse `reason` 인자 `nargs="*"` (0 또는 N개 수용, backward compat). 신규 `_sanitize_reason()` 헬퍼 — 공백 join / `\r\n` → 공백 collapse / `\|` markdown escape / strip 후 빈 문자열 fallback. apply 분기: 사유 인자 있으면 draft row 내 placeholder 치환 + 적용 메시지에 사유 echo, 없으면 placeholder 유지 + 사용자 manual 편집 유도 안내. `.claude/commands/cm-claudemd-apply.md` UX 본문 갱신 — `argument-hint: <session_id> [사유...]`, 인자·사용 예시·안전 처리 표(인자 0개/`\|`/줄바꿈/빈 문자열/placeholder 부재) 명시. unit test (`_sanitize_reason` 5 케이스) + argparse `nargs='*'` 검증으로 회귀 가드. | (1) 기존: 사유 컬럼이 placeholder인 채로 CLAUDE.md에 적용되고, 사용자가 적용 후 vim/IDE로 직접 편집해야 했음 — 잊으면 변경 이력 row 무의미해지는 *silent failure*. (2) 신규: `/cm-claudemd-apply <sid> 사유 한 줄`로 즉시 치환 가능 + 인자 0개 시 기존 동작 그대로 보존. (3) markdown table 안전 처리(`\|` escape) 내장 — 사유에 pipe 들어와도 표 깨짐 없음. (4) Slash command·argparse·문서 3 영역 정합 — placeholder 상수가 `_schema.py` 단일 출처라 향후 변경 시 sync drift 방지. dharness 본체 read-only invariant 보존. |
| 2026-05-10 | Phase 5-2 7차 사이클 — read-only 진단 진입점 `/harness:harness-mcp-status` 추가 (쓰기/읽기 한 쌍 완성) | `plugins/harness/commands/harness-mcp-status.md` 신설 — 5섹션 진단(① `claude mcp list` 인벤토리 / ② 에이전트별 도구 매트릭스 — `tools:` frontmatter 파싱 + capability profile 추정 / ③ `permission-profiles.md` §3 박제 vs 실제 diff / ④ `settings.json` permissions 정합 — 잉여·결손·승급 정책 위반 / ⑤ §10-1 trigger 자동 감지 — baseline-diff·profile-mismatch·사용자-요청). 자동 수정 0(read-only), `--verbose` 플래그로 §3 dump·도구 enumeration 확장. SKILL.md Phase 5-2 단락 *런타임 시점* 줄에 "채택 *전후* 진단" cross-link 1줄 추가, `harness-mcp-adopt.md` 후속 명령어에 1줄, `harness-add-agent.md` 범위 외에 1줄, `harness-new.md` 후속 명령어에 1줄 추가. README 슬래시 카탈로그 8→9, 합계 14→15로 갱신. | (1) `harness-mcp-adopt`(쓰기) ↔ `harness-mcp-status`(읽기) 한 쌍이 §10 dynamic adoption의 진입/관찰 채널을 분리해 *채택 전 후보 검토*와 *채택 후 정합 점검* 모두 명시 호출 가능. (2) §10-1 trigger 신호 (a) baseline-diff와 (b) profile-mismatch는 *deterministic*하게 감지 가능(코드 grep + frontmatter 파싱)이라 description 매칭에 의존하지 않음 — `--verbose`로 출력 dump해 사용자가 채택 결정. (3) settings.json `allow`로 부수 효과 도구가 승급되는 정책 위반 패턴(§6 위반 후보)을 카탈로그화하여 검출. 권고 수준만 — 자동 강등 금지. (4) dharness 본체 read-only invariant 보존 — 편집은 `plugins/harness/` 내부 1 신설 + 4 cross-link 갱신, 외부 변경 0. |
